export default function ItemCard({data}){


    return(
        <div>
            { data.map((currentItem)=>(
                <li> 
                    
                </li>
            ))}
        </div>
    )
}